# Origami DI Probe — dependances
# Bornes choisies pour la reproductibilite (CPU, Windows/Linux/macOS).
# Apres premiere install reussie : `pip freeze > requirements.lock.txt`
# pour figer les versions exactes (pre-enregistrement).

numpy>=1.24,<2.0
torch>=2.1
transformers>=4.38
scikit-dimension>=0.3.0
scikit-learn>=1.3
pandas>=2.0          # dependance transitive de scikit-dimension (skdim.datasets)
matplotlib>=3.7      # plot_results.py (visualisation, hors verdict)
